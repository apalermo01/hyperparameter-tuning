name: hparam_project
channels:
    - defaults
    - conda-forge
dependencies:
    - numpy
    - pandas
    - matplotlib
    - jupyter
    - scikit-learn
    - pytorch
    - torchvision
    - pip
    - pip:
        - opencv-python
        - pytorch-lightning==1.6
        - ray[tune]
        - tqdm
        - boto3
